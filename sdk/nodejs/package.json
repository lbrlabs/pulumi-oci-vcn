{
    "name": "@lbrlabs/pulumi-oci-vcn",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource oci-vcn ${VERSION}"
    },
    "dependencies": {
        "@pulumi/oci": "^0.5.0"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true,
        "name": "oci-vcn"
    }
}
